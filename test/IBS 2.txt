

A = [1,5,7,20,2,1,5,21,7,5]
 
def method(A):
  for i in set(A):
     print(f'count of {i} is {A.count(i)})

method(A)


Python interpreters translate each line of code line by line, 
whereas compilers translate the entire code at once. Despite its slowness, 
it is one of the most widely used programming languages.

rdd1 = sc.textFile('file')

rdd2 = rdd1.flatMap(lambda x: x.split()).map(lambda x:(x,1)).reduceByKey(lambda a,b:a+b)

rdd2.collect()


select * into table2 from (select top 0

